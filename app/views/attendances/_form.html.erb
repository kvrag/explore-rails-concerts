<%= form_for :attendance, url: attendances_path, method: :post, remote: true do |f| %>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <%= f.hidden_field :concert_id, value: concert.id %>
  <%= button_tag "Attend", class: "button attend" %>
<% end %>