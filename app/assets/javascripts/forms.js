$(document).ready(function () {
 
  $("#attendee-list")
    .on("ajax:success", (
      this.append(response),
      $("#attend").remove())

   ).on("ajax:error", (this.after(xhr.responseJSON.html)
   );

})
 